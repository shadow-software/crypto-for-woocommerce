# Composer / dev dependencies (the shipped plugin bundles nothing)
/vendor/
composer.phar

# Node / build
/node_modules/
/build/
/dist/
*.zip

# Local wp-env dev site (used only to capture screenshots)
/.wp-env.json
/.wp-env.override.json

# Test + tooling caches
.phpunit.cache/
.phpunit.result.cache
.phpcs.cache
phpcs.xml
phpstan.neon.local
.php-cs-fixer.cache

# Logs
*.log

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.sublime-*
*~

# Agent tooling — canonical copy lives in shadow-agent-markdown, never this repo
.claude/
.agents/
